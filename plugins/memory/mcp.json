{
  "mcpServers": {
    "memory": {
      "command": "node",
      "args": [
        "${userHome}/.cursor/plugins/local/memory/server/dist/index.cjs"
      ],
      "env": {
        "MEMORY_VAULT_PATH": "${env:MEMORY_VAULT_PATH}"
      }
    }
  }
}
